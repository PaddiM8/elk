pub fn addition(x, y): x + y
pub fn subtraction(x, y): x - y
pub fn multiplication(x, y): x * y

pub module sub {
    pub struct PublicStruct(a, b);
    struct PrivateStruct(a, b);

    pub fn division(x, y): x / y

    new sub::PrivateStruct(1, 2)
}

new sub::PublicStruct(1, 2)
